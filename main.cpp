#include <iostream>

int main() {
    // std::cout is the object used to display output on the console.
    // The insertion operator (<<) is used to send the string to the output stream.
    std::cout << "Hello World!" << std::endl;
    
    // Returning 0 indicates that the program executed successfully.
    return 0;
}
